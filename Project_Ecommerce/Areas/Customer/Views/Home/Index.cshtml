@model IEnumerable<Project_Ecommerce.Models.Product>
@*<div class="row">

    @foreach (var product in Model)
    {

        <div class="col-lg-3 col-md-6">
            <div class="row p-2">
                <div class="col-12  p-1" style="border:1px solid #008cba; border-radius: 5px;">
                    <div class="card" style="border:0px;">
                        <img src="@product.ImageUrl" class="card-img-top rounded"  style="height:350px">
                        <div class="pl-1">
                            <p class="card-title h5"><b style="color:#2c3e50">@product.Title</b></p>
                            <p class="card-title text-primary">by <b>@product.Author</b></p>
                        </div>
                        <div style="padding-left:5px;">
                            <p>List Price: <strike><b class="">₹ @product.ListPrice.ToString("0.00")</b></strike></p>
                        </div>
                        <div style="padding-left:5px;">
                            <p style="color:maroon">As low as: <b class="">₹ @product.Price100.ToString("0.00")</b></p>
                        </div>
                    </div>
                    <div>
                        <a asp-action="Details" class="btn btn-primary form-control" asp-route-id="@product.Id">Details</a>
                    </div>
                </div>
            </div>
        </div>

    }
</div>
<h6>OR</h6>*@

<div class="col-12 row border-bottom">
    <h2 class="text-primary">Available Products</h2>
</div>

<div class="row m-2 p-2">
    <div class="column">
        <table id="tbldata">
            <tbody></tbody>
        </table>
    </div>
</div>



@section scripts{
    <script src="~/js/Demo.js"></script>
    <style>
        .column {
            width: 100%;
        }

        tbody {
            display: grid;
            grid-template-columns: repeat(4,auto);
            max-width: 100%;
        }
    </style>
}
